import 'package:flutter/material.dart';

class GradeBook extends StatelessWidget {
  const GradeBook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Gradebook Page');
  }
}